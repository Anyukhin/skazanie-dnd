# Завершение 3D-плана — 12 сентября 2026

Ветка `codex/environment-asset-pipeline`. Завершены оставшиеся работы по
окружению, шести профилям фигурок, крышам и серверной площади крупных существ.
Финальный `pnpm verify` пройден: 12 бюджетных, 3700 функциональных и 2 MVP-теста,
проверки типов сервера и клиента, production-сборка. Всего 3714 тестов, без пропусков.

## Реализовано

| Этап | Результат |
| --- | --- |
| A0/A1 | Отдельный кандидат, проверяемые исходники и права, общий атлас, неизменяемые URL, ревизия на карту и сохранённый baseline `pr79` |
| B | Общий ограниченный кэш GLB, независимая отмена запросов, сохранение окружения при неизменной карте, замена отдельной фигурки |
| C | 177 моделей: 135 Quaternius/Kenney и 42 оригинальных; 94 вида с GLB и 11 плоских видов, всего 105 |
| D1 | Путник, следопыт, маг, скелет, гоблин и волк; старые ключи и KayKit сохранены; внешность передаётся разрешённым серверным профилем |
| D2 | Комплекты оружия, реальные клипы, поза лука, правильные крепления и масштаб аксессуаров, снимок атаки и начало траектории в позиции атакующего |
| D3 | Серверная площадь 1×1–4×4 для движения, дистанции, LOS, областей, размещения и переходов; согласованные 2D/3D центры и выбор по занятой площади |
| E1 | Объёмный выбор, состояния крышек, опоры утвари и настенные крепления |
| E2 | Скатные крыши и каменные своды; режимы среза, полного и скрытого вида; в срезе свода остаются крайние арки |
| F | Три профиля качества, остановка неподвижной сцены в экономном режиме, контроль кэша и освобождение GPU-ресурсов |

Выпуск окружения — `bd5c4563074b6e4ef576769c`. Модели и атлас занимают
около 63,2 МиБ в проверяемом кандидате при лимите 64 МиБ. У двенадцати тяжёлых
предметов цветовая текстура уменьшена с 512 до 256 пикселей; геометрия,
normal/ORM, анимации и петли сохранены. Экономия — 5 305 964 байта.

Четыре новые фигурки находятся в `quaternius/actors-b892de8fd4f015796032/`.
Внутри сохранены исходные лицензии и происхождение с SHA-256. Маг использует
цельную модель в капюшоне с изменённой палитрой ткани. Скелет и гоблин сохраняют
исходные скелеты и анимации; размер головы скорректирован при подготовке.
Оружие учитывает масштаб исходного скелета, посох держится рукоятью в ладони.

## Совместимость и правила

`actor.footprint = { version: 1, size }` хранит сторону квадратной площади.
Размер назначается сервером; отсутствие поля сохраняет прежнюю площадь 1×1.
Смена фигурки не меняет размер. Крупные NPC, противники и призывы используют
общую геометрию. При переходе вся площадь каждого участника резервируется,
поэтому тела не накладываются. Края закрытых дверей учитываются при размещении
и проверке траекторий. Атака по скрытой цели через HTTP отклоняется; ход врага
не раскрывает игроку туман. Частично скрытый footprint врага и параметры
скрытой местности не выдаются проекцией.

Сохранённые карты продолжают использовать свои ревизии. Старые `pr79` и
`a544cc0e08820485ad80203d` не переписаны. Новые ассеты применяются к новым
картам; ради оформления рабочие сохранения не мигрируют.

## Приёмка

На временном сервере два пользователя вошли в одну комнату. Первый выполнил
атаку мечом в 3D по краю существа 2×2, второй — выстрел в 2D. Дополнительные
API-атаки проверили идемпотентность: точный повтор не добавил события и не
потратил второй боеприпас. После четырёх атак осталось 18 стрел, у цели —
92 ОЗ. Обе проекции содержали одинаковую площадь и хронику.

После перезапуска сохранились версия 10, четыре атаки, 18 стрел, здоровье цели
и концы траекторий. Повтор команды снова вернул `idempotent_replay`.
Replay без снимков совпал с загруженным состоянием. Рабочее `storage/` не менялось.

Все новые предметы просмотрены сверху и в объёме. Проверены целостность мебели,
колесо, крыша колодца, фактуры еды и дерева; обычные генераторы таверны, склепа
и леса; крыши, своды, фигуры с оружием и позы. Целостность 2013 ассетов прошла
`pnpm content:verify`. Эта проверка не заменяет общую проверку прав всего проекта.

## Измерения

Windows, RTX 5080, Chromium/ANGLE D3D11, Three r186. Обычный генератор `building`,
seed `qa-3d-final-building`, 26×26, три фигуры, одна с footprint 2×2.
Canvas 1150×650, DPR 1. Диагностика CPU после прогрева:

| Качество | Среднее CPU на кадр | CPU p95 | Draw calls | Треугольники с проходами теней |
| --- | ---: | ---: | ---: | ---: |
| Высокое | 7,9 мс | 9,3 мс | 1883 | 1 203 154 |
| Обычное | 2,5 мс | 3,0 мс | 437 | 322 616 |
| Экономное | 1,2 мс | 1,7 мс | 223 | 163 576 |

Первые кадры загрузки и загрузка текстур на GPU в эту таблицу не включены.
Это замер CPU, а не GPU или частоты монитора. В экономном режиме после
выключения FPS счётчик кадров оставался 77067 при видимой вкладке.

После 50 переходов между тремя сценами ресурсы каждой сцены после прогрева
оставались постоянными:

| Сцена | Геометрии | Текстуры renderer.info | Настоящие дескрипторы текстур GPU |
| --- | ---: | ---: | ---: |
| Таверна | 195 | 154 | 158 |
| Склеп | 123 | 44 | 48 |
| Лес | 80 | 36 | 40 |

После шестого перехода счётчик загрузок оставался 63, кэш — 62 записи,
28 996 992 байта. Проверка проведена с обычным качеством и срезом крыши.
Изменение толщины и цвета крайних арок не меняет число ресурсов.

## Границы плана

Следующая задача заменила условные комплекты [видимой экипировкой предметов](visible-equipment.md).
Остались отдельные этажи карт и шесть базовых профилей существ.
Полёт, физика тел, объёмная LOS, перекрывающиеся этажи,
редактор лица/расы и отдельная модель каждого монстра в этот план не входят.
Наборы исходных анимаций различаются; при отсутствии клипа используется
совместимая стойка, для лука предусмотрена собственная поза прицеливания.

Подробности: [модели](3d-models.md), [генерация](3d-generation.md),
[105 видов реквизита](3d-prop-coverage.md), [покрытие правил](rules-coverage.md).
