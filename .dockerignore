node_modules
.pnpm-store
dist
.git
.env
storage
*.log
*.tsbuildinfo

# Рабочие каталоги генерации иконок: сотни промежуточных PNG на полтора гигабайта.
# Без этих строк docker отправлял демону весь мусор — контекст рос до 2,5 ГБ и
# один только «transferring context» съедал две с половиной минуты сборки.
tmp
assets-src

# Листы сверки нарисованных иконок. В приложении на них никто не ссылается,
# но vite копирует public/ целиком, поэтому они утяжеляли и образ, и dist.
public/assets/test

.agent-tasks

# Служебное хозяйство агентов. В .claude/worktrees лежат целые копии дерева —
# треть гигабайта, которой в образе делать нечего.
.claude
.github
docs
eval/*
!eval/combat-lab.mjs
tools
test
